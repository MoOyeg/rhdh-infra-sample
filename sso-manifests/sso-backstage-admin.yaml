apiVersion: keycloak.org/v1alpha1
kind: KeycloakUser
metadata:
  name: backstage-admin-user
  namespace: ${SSO_NAMESPACE}
spec:
  user:
    username: "backstage-admin"
    email: "backstage-admin@test.com"
    enabled: True
    emailVerified: True
    credentials:
      - type: "password"
        value: "test"
        temporary: false
    realmRoles:
      - "offline_access"
      - "uma_authorization"
      - "user"
  realmSelector:
    matchLabels:
      app: sso-backstage-sample
#Username must be set to email due to bug https://issues.redhat.com/browse/RHIDP-2272